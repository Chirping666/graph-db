[package]
name = "graph_db"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = ["alloc", "libc"]
alloc = []

[dependencies]

[target.'cfg(unix)'.dependencies]
libc = { version = "0.2", optional = true }

[dev-dependencies]
tempfile = "3"
